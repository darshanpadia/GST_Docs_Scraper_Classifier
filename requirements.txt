requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
PyMuPDF>=1.24
pytesseract>=0.3.10
Pillow>=10.0
python-dotenv>=1.0
Flask>=3.0

# Only required if ENABLE_LLM_FALLBACK=true (see .env.example). gemini/groq
# are the free-tier providers enabled by default; anthropic is optional
# (isn't free) and not in the default LLM_PROVIDER_ORDER.
google-genai>=1.0
groq>=0.11
anthropic>=0.40

# dev / test
pytest>=8.0
